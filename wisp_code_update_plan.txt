This file details modifications and plans to update and extend the Wisp code generated by Maxwell Loughan in the Summer of 2026.

Ideas:
    Move big arrays to JSON, hdf5, or other file structures that get loaded in
        Port Names array in HSX_probe_field.py - DONE
        port dictionary in HSX_probe_field.py - DONE
        check more of them.
    Restructure HSX_probe_field.py with the main block

    modularity - possible methods/functions to define to reduce complexity
        mesh and grid loading
        model loading
    Add in functionality to test multiple probe lengths (also from command line)
        He does have a commented line for input from command line
    pyvista gui additions:
        Add North, South, East, and West markers on the gui in pyvista
        Mark already occupied ports
        Add all ports (idk if this is not the case.)
    Conventional Changes
        Determine how HSX quadrants and ports are actually named (if there is a naming scheme)
            if no naming scheme: determine if Max's is liked/useful and logical.
    
    