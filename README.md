# HSX WISP Gauges
This file details modifications and plans to update and extend the Wisp code generated by Maxwell Loughan in the Summer of 2026.

Ideas:
    Move big arrays to JSON, hdf5, or other file structures that get loaded in
        Port Names array in HSX_probe_field.py - DONE
        port dictionary in HSX_probe_field.py - DONE
        check more of them.
    Restructure HSX_probe_field.py with the main block

    modularity - possible methods/functions to define to reduce complexity
        mesh and grid loading
        model loading
    Add in functionality to test multiple probe lengths (also from command line)
        He does have a commented line for input from command line
    pyvista gui additions:
        Add North, South, East, and West markers on the gui in pyvista
        Mark already occupied ports
        Add all ports (idk if this is not the case.)
    Conventional Changes
        Determine how HSX quadrants and ports are actually named (if there is a naming scheme)
            if no naming scheme: determine if Max's is liked/useful and logical.

Plan Moving forward (As of 09/03/2026):
Refactor code as full project: 
magnetic_analysis/
│
├── data/                            # Raw data & config inputs
│   ├── max_port_positions.json      # Port positions and geometry specs
│   └── models/                      # Raw .mgrid or dataset files
│
├── cache/                           # Auto-generated pre-computed binary files (.npy)
│
├── output/             # User-generated script deliverables
|   ├── plots/          # Rendered PyVista screenshots, PNGs, and interactive HTML 3D views
|   ├── exports/        # Calculated field vectors, CSV summary tables, exported meshes
│   └── logs/           # Performance profiles, run outputs, and debug logs
├── src/magnetic_analysis/           # Core library package
│   ├── __init__.py                  # Top-level API exports for end users
│   ├── models.py                    # OOP: MagneticModel & PortRegistry classes
│   ├── visualizer.py                # OOP: PyVista Plotter & UI event callbacks
│   ├── geometry.py                  # Modular: Pure NumPy/PyVista vector math & flood-fill
│   └── io.py                        # Modular: Fast binary caching & file loading
│
├── scripts/                         # Clean entry points for collaborators
│   ├── run_visualization.py         # 5-line interactive viewer script
│   └── compare_models.py            # Direct Python comparison script (no subprocesses)
│
├── pyproject.toml                   # Dependencies and environment setup
└── README.md                        # Quickstart instructions  